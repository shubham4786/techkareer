// change this if needed.
export type Job = {
  id: number;
  name: string;
  companyName: string;
  oneLiner: string;
  description: string;
  logo: string;
  payRange: string;
  role: string;
  type: string;
  location: string;
  jobId: string;
  createdAt: string;
};

export type Talent = {
  ID: number;
  "Introduction / Pitch": string;
};

